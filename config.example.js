// Copy this file to `config.js` (gitignored) before running.
//
// DEMO_MODE = true seeds state from lib/fixtures.js and skips Supabase entirely —
// useful for previewing the UI locally without a backend. Flip to false and fill in
// real Supabase credentials when ready to use cross-device sync.

export const DEMO_MODE = true;

export const SUPABASE_URL = 'https://YOUR-PROJECT.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';
